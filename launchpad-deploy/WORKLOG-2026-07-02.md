# Launchpad GB300 (rg-forge-launchpad) — Work Log, 2026-07-02

**Site:** `rg-forge-launchpad` (NVL72, org `ncx`) · **Branch:** `launchpad-deployment`

**Outcome:** NICo REST + Flow fully deployed and healthy; site registered, paired, and **online**. All namespace pods verified clean (nico-system, nico-rest, flow, temporal, postgres).

## Done
- **Site registration (siteId):** registered the site in the REST DB (was missing) — `GET /v2/org/ncx/nico/site` now returns it; site paired (`Registered`, `isOnline: true`).
- **REST + Flow upgraded v1.6.0 → v2.1.0-pr-14:** whole stack (api, site-manager, site-agent, workers, flow/psm/nsm) on one image; schema migration clean.
- **NICo Flow deployed + connected:** flow/psm/nsm 3/3 Running; site-agent↔Flow mTLS connection established (`FLOW_GRPC_ENABLED=true`).
- **Machine-validation fix (per Krish):** disabled the failing `forge_DcgmFullLong` DCGM test that was blocking GB300 trays from Ready — applied live + verified.
- **Deploy config git-tracked:** consolidated onto `launchpad-deploy/` (branch `launchpad-deployment`), synced to live state, secret-free (creds via Vault); added reusable scripts `register-rest-site.sh` + `deploy-flow.sh`.

## Key issues fixed
- **Temporal backend DB (`postgres-0`) undersized** (512Mi) → persistence query cancellations stalled workflows → site wouldn't pair. Scaled to 4Gi/2cpu — resolved (this was the root cause of the pairing hang).
- **Flow v1.6→v2.1 migration-hash mismatch** → reset the (empty) flow DB so v2.1 migrated fresh.
- **site-agent v1.6 metrics-panic crashloop** when Flow enabled → fixed by the v2.1 image.

## Blocked / follow-up
- **Rack + component-manager config (Jira "Setup rack and component manager configs"):** siteConfig applied, but **RMS is not deployed** (`rms-api-server.rack-manager`) → switch/power-shelf rack ops pend until RMS is stood up. → RESOLVED 2026-07-08: RMS deployed & working
  (see RMS-ENABLEMENT.md); REST/Flow upgraded to v2.1, site registered, rack config applied,
  ssh-console-rs configured — see WORKLOG-2026-07-08.md.
- Cosmetic (non-blocking): GB300 BMC leak-detector 401s + `DEVICE_TYPE_UNKNOWN` capability warning (hardware/firmware quirks).
- `launchpad-deployment` branch not yet pushed.

## Reference
- siteId `8c894583-bea4-445d-a5bd-46ee0e3cb3fb` · org `ncx` · provider `6558fca2-f07e-439b-ae32-bba1d8169380`
- REST/Flow image `v2.1.0-pr-14-g0d5452b9a` · Flow ns `flow`
- Keycloak token helper: `helm-prereqs/keycloak/get-token.sh` (client `ncx-service`, realm `nico`)
- Runbooks: `launchpad-deploy/nico/{register-rest-site,deploy-flow}.sh`, `RACK-CONFIG.md`, `V2-UPGRADE-PLAN.md`
