# Launchpad GB300 (rg-forge-launchpad) — Work Log, 2026-07-02 → 2026-07-08

Overview of what was done after `WORKLOG-2026-07-02.md`. Full details live in the per-layer docs; this is
the "what changed + why" index. All config is captured in `nico/nico-core.launchpad.yaml` + the scripts.

## Done

- **REST + Flow upgraded v1.6.0 → `v2.1.0-pr-14-g0d5452b9a`.** The v1.6 site-agent panicked
  (`duplicate metrics collector registration`) whenever `FLOW_GRPC_ENABLED=true`, blocking agent↔Flow; the
  v2.1 image fixes it. `install-rest-1.6.0.sh` is now pinned to this tag (filename is legacy). See
  `nico/V2-UPGRADE-PLAN.md` (marked complete).

- **REST site registered.** New site UUID **`8c894583-bea4-445d-a5bd-46ee0e3cb3fb`** (org `ncx`), replacing
  the orphaned `7f91b08b-…`. Automated in `nico/register-rest-site.sh`. Site is **Registered / isOnline:true**.

- **NICo Flow deployed** (ns `flow`: nico-flow/psm/nsm + Temporal `flow` namespace), agent↔Flow enabled
  (`FLOW_GRPC_ENABLED=true`) via `nico/deploy-flow.sh`. See `FLOW-FIXES.md`.

- **Temporal Postgres sizing fix (the real site-pairing unblocker).** `postgres-0` (ns `postgres`) was
  starving at 512Mi → Temporal `ReadHistoryBranch` statement-cancels → workflows stalled → site wouldn't
  pair. Bumped to **requests 2Gi/1cpu, limits 4Gi/2cpu**
  (`rest-api/deploy/kustomize/base/postgres/statefulset.yaml`). Worker CPU bumps did not help — DB was the bottleneck.

- **RMS deployed and working** (`rms-api-server` / ns `rack-manager`). NICo Core→RMS is mTLS via the
  `vault-nico-issuer` SPIFFE certs. Confirmed: RMS logs `BatchGetPowerState status=200 peer="CN=nico-api…"
  rack=launchpad-r1`. See `RMS-ENABLEMENT.md` + `RMS-RUNBOOK.md`.

- **Rack + component-manager siteConfig applied** (Jira "Setup rack and component manager configs"):
  `[site_explorer]` (create_switches/create_power_shelves, explore_mode=nv-redfish), `[rack_profiles.NVL72_GB300]`
  (compute 18/Lenovo, switch 9/NVIDIA, power_shelf 6/LiteOn), `[component_manager]` (rms backends), `[rms]`
  (HTTPS + mTLS to rms-api-server.rack-manager.svc:8801). Rack `launchpad-r1` → state `Created`. Also disabled
  a failing MV test: `tests=[{id="forge_DcgmFullLong",enable=false}]`. See `nico/RACK-CONFIG.md`.

- **`deny_prefixes` emptied → `[]`** to match live (was the 7-CIDR complement of `172.16.2.0/24`). Live is
  source-of-truth; keeps the mgmt-underlay VIPs reachable and avoids the DPU HBN drop.

- **Healed the stuck `nico` helm release.** Revs 15–18 had failed on a server-side-apply field-ownership
  conflict (`nico-api-site-config-files` owned by a prior `kubectl edit`). Healed with Helm 4
  `helm upgrade … --force-conflicts` → **rev 19 deployed**. Pre-flight diff confirmed no live drift would be
  reverted (services' ETP, images, nico-pxe pod spec all matched). See README §12.

- **ssh-console-rs (cert serial console) configured and working.** Added `nico-ssh-console-rs.configFiles.config`
  with the fleet-wide nvcert CA fingerprint `SHA256:sPKz…` + role `swngc-forge-admins`; VIP `172.16.2.49`.
  Confirmed end-to-end: `certificate auth succeeded … in role swngc-forge-admins`, SOL console attached. Key
  gotcha: the chart has no config checksum → **must `rollout restart deploy/nico-ssh-console-rs` after any
  config change** (stale pod = "CA not trusted"). Full playbook in `SSH-CONSOLE.md`.

- **Docs refreshed** (this pass): README rewritten (versions, deploy order, Helm-4 heal, all layers);
  new `SSH-CONSOLE.md`; CONFIG-GUIDE/NETWORKING/PHASE4/RACK-CONFIG/fills/V2-UPGRADE reconciled to live
  (image tags, site UUID, deny_prefixes=[], self-hosted DHCP relay, ssh-console-rs naming, mTLS `[rms]`).

## Task status
Jira/local tasks #18–#27 complete (v2 upgrade, site registration, Flow, RMS, rack config, ssh-console).

## Open follow-ups (not blocking; separate from the config-setup tickets)
- **Switch ingestion:** 6/9 switches in AvoidLockout → verify BMC creds + `site-explorer refresh <ip>`.
- **LiteOn power shelves:** "Unsupported BMC vendor" — needs a NICo LiteOn handler or explicit exclusion.
- **Rack-id alignment:** trays report `RackIdentifier: gb300-01` vs rack `launchpad-r1`.
- **ssh-console-rs chart:** add a `checksum/config` annotation so config changes auto-restart the pod
  (removes the manual `rollout restart` step) — see `SSH-CONSOLE.md` §3.
- **Commit:** fold the `nico-core.launchpad.yaml` + doc updates into the `launchpad-deployment` single commit.
