# nvcert worklog — 2026-07-30

## RMS deploy prep + site-config reconcile

### Site-config reconcile (nico-core.nvcert.yaml)
The tracked `nico/nico-core.nvcert.yaml` had drifted from what's deployed. Adopted the
live/source-of-truth copy (site name, hostname, domain, image tag, ntp_servers all filled;
`[nvlink_config] enabled = true` added), then re-added two things the live copy was missing:
- `{ id = "forge_DcgmFullShort", enable = false }` in `[machine_validation_config].tests`
  (EnableAll mode re-enables it on every nico-api restart otherwise; DCGM tests fail until
  the NVLink fabric is up).
- `unbound.localData` entries `grafana.forge` (.31) + `otel-receiver.forge` (.30).
Also corrected two stale DHCP comments (relay is on the mgmt switches, not cp-1).
TOML re-validated OK. This changes the tracked file only — nothing was pushed to the cluster.

### RMS (Rack Manager Service) — prepared, deploy is manual
Added `launchpad-nvcert/rms/`:
- `nvcert-values.yaml` — Helm values, copy of launchpad's with image tag
  `v0.9.0-dev1-116-g8e7aaf0-amd64`, memory DB, mTLS on (existingSecret rms-api-server-certificate).
- `rms-api-server-certificate.yaml` — cert-manager Certificate (vault-nico-issuer).
- `README.md` — full manual run-book.

Deploy is run manually by the operator (see rms/README.md). Summary:
1. `kubectl create namespace rack-manager`; apply the Certificate; apply the
   nvidian/dcim-scoped `image_pull_secret_rms.yaml` (gitignored — holds a real token).
2. Append `secrets/{data,metadata}/racks*` capabilities to the Vault `forge-vault-policy`.
3. `git checkout 8e7aaf0` in the rackmanagementservice repo; `helm upgrade --install
   rack-manager ./helm -f launchpad-nvcert/rms/nvcert-values.yaml -n rack-manager`.
4. Verify: RMS log `BatchGetPowerState` 200, peer CN=nico-api…; nico-api no rms:8801 errors.

### Verified (read-only, on nvcert cluster)
- `vault-nico-issuer` ClusterIssuer Ready; `rack-manager` namespace absent.
- Live CM `nico-api-site-config-files` already carries `[rms]`, `[component_manager]`,
  `[rack_profiles.NVL72_GB300]` → no NICo change needed for RMS.
- nvcert REST siteId = `30b7f861-2b28-4da7-89b1-94d0e984457a` (for expected-rack, rackId `nvcert-r1`).

### Follow-up (after RMS healthy)
Rack + NVLink switch + power-shelf ingestion (verify switch BMC MACs vs ToR fdb), then
NVLink fabric bring-up → re-enable `forge_DcgmFullShort`.
